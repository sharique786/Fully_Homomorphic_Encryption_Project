apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fhe-server.fullname" . }}-secret
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "fhe-server.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets.data }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}